#include <bits/stdc++.h>

using namespace std;

vector<int> nums;

int main() {
    int num;
    while (cin >> num) {
        nums.push_back(num);
    }
    int ret;

    // wait to be solved
    
    cout << ret << endl;

    return 0;
}